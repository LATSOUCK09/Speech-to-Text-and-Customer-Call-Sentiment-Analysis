services:
  speech-sentiment-api:
    build: .
    container_name: speech-sentiment-api
    ports:
      - "8000:8000"
    environment:
      HOST: 0.0.0.0
      PORT: 8000
    restart: unless-stopped
